// Vehicles publicly plannable as "Coming Soon" on the Home page's All
// Cars section (see src/home.js), but without a src/cars/<slug>.js file
// yet. Deliberately empty -- we don't invent unannounced roadmap items.
// Add an entry once a next vehicle is actually decided:
//   { maker: 'Toyota', name: 'Prius' }
// and remove it again once that vehicle gets its own real data file.
export default []
